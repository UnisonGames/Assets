using UnityEngine;
using System.Collections;

public class AI_Strong : MonoBehaviour {

	// Use this for initialization
	public enum states { idle, aggressive, neutral, dead };
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
